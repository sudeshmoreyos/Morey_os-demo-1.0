ifdef FREQ
	F_CPU = $(FREQ)
	CDEFS += -DCONTROLLER_FREQ_CONF=$(FREQ)UL
else
	F_CPU = 20000000
    #$(warning WARNING : Default 20MHz frequency is chosen for atmega4809 controller)
endif

MCU = atmega4809

ifndef MCU_AVRDUDE
	MCU_AVRDUDE = atmega4809
endif